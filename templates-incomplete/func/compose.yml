# This template is based on an external compose file
# Repository: https://github.com/openfaas/faas
# Stack file: docker-compose.yml
#
# Please refer to the repository for the complete docker-compose configuration
# Or manually create your compose configuration below

version: '3.8'

services:
  func:
    # Configure your service here
    # image: your-image:tag
