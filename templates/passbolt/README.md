# Passbolt

Passbolt is a free and open source password manager designed for collaboration. With Passbolt you can securely generate, store, manage and monitor your team credentials. Get access to all of your logins and passwords from multiple browsers or even your mobile phone.

![Logo](https://raw.githubusercontent.com/pi-hosted/pi-hosted/master/images/passbolt.png)

## Docker Image

`undefined`

## Categories

- Other
- Tools

## Environment Variables

- `PUID` - PUID (default: `1000`)
- `PGID` - PGID (default: `1000`)
- `TZ` - TZ (default: `Europe/Zurich`)
- `MYSQL_ROOT_PASSWORD` - MYSQL_ROOT_PASSWORD
- `MYSQL_DATABASE` - MYSQL_DATABASE (default: `passbolt`)
- `MYSQL_USER` - MYSQL_USER (default: `passbolt`)
- `MYSQL_PASSWORD` - MYSQL_PASSWORD
- `PASSBOLT_PORT` - PASSBOLT_PORT (default: `443`)
- `PASSBOLT_URL` - PASSBOLT_URL (default: `https://passbolt.local`)
- `EMAIL_FROM_NAME` - EMAIL_FROM_NAME (default: `Passbolt`)
- `EMAIL_FROM_ADDRESS` - EMAIL_FROM_ADDRESS
- `EMAIL_SMTP_SERVER` - EMAIL_SMTP_SERVER (default: `smtp.gmail.com`)
- `EMAIL_SMTP_PORT` - EMAIL_SMTP_PORT (default: `587`)
- `EMAIL_USERNAME` - EMAIL_USERNAME
- `EMAIL_PASSWORD` - EMAIL_PASSWORD
- `EMAIL_TLS` - EMAIL_TLS (default: `true`)

## Maintainer

 https://github.com/novaspirit/pi-hosted/

